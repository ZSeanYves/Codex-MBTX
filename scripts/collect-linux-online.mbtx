import {
  "moonbitlang/async@0.21.3",
  "moonbitlang/async@0.21.3/shell",
  "moonbitlang/core/env",
}

async fn main {
  @shell.Cmd("moon", ["run", "scripts/collect.mbtx", "codex-relay"] + @env.args()[1:].to_owned()).run()
}
