diff --git a/codex-rs/app-server/src/in_process.rs b/codex-rs/app-server/src/in_process.rs
index d8613e9..371c48d 100644
--- a/codex-rs/app-server/src/in_process.rs
+++ b/codex-rs/app-server/src/in_process.rs
@@ -559,6 +559,7 @@ async fn start_uninitialized(args: InProcessStartArgs) -> IoResult<InProcessClie
                 }
             }
 
+            codex_utils_pty::trace::emit("codex_shutdown", "processor_cleanup_begin", "{}");
             processor.clear_runtime_references();
             processor.cancel_active_login().await;
             processor
@@ -567,6 +568,7 @@ async fn start_uninitialized(args: InProcessStartArgs) -> IoResult<InProcessClie
             processor.clear_all_thread_listeners().await;
             processor.drain_background_tasks().await;
             processor.shutdown_threads().await;
+            codex_utils_pty::trace::emit("codex_shutdown", "processor_cleanup_return", "{}");
         });
         let mut pending_request_responses =
             HashMap::<RequestId, oneshot::Sender<PendingClientRequestResponse>>::new();
@@ -760,17 +762,23 @@ async fn start_uninitialized(args: InProcessStartArgs) -> IoResult<InProcessClie
             )));
         }
 
+        codex_utils_pty::trace::emit("codex_shutdown", "processor_join_begin", "{}");
         if let Err(_elapsed) = timeout(SHUTDOWN_TIMEOUT, &mut processor_handle).await {
             processor_handle.abort();
             let _ = processor_handle.await;
         }
+        codex_utils_pty::trace::emit("codex_shutdown", "processor_join_return", "{}");
+        codex_utils_pty::trace::emit("codex_shutdown", "outbound_join_begin", "{}");
         let _ = outbound_shutdown_tx.send(());
         if let Err(_elapsed) = timeout(SHUTDOWN_TIMEOUT, &mut outbound_handle).await {
             outbound_handle.abort();
             let _ = outbound_handle.await;
         }
+        codex_utils_pty::trace::emit("codex_shutdown", "outbound_join_return", "{}");
 
+        codex_utils_pty::trace::emit("codex_shutdown", "analytics_flush_begin", "{}");
         analytics_events_flush_client.flush().await;
+        codex_utils_pty::trace::emit("codex_shutdown", "analytics_flush_return", "{}");
 
         if let Some(done_tx) = shutdown_ack {
             let _ = done_tx.send(());
diff --git a/codex-rs/app-server/src/request_processors/thread_processor.rs b/codex-rs/app-server/src/request_processors/thread_processor.rs
index 56eb6fd..66a0b1b 100644
--- a/codex-rs/app-server/src/request_processors/thread_processor.rs
+++ b/codex-rs/app-server/src/request_processors/thread_processor.rs
@@ -528,6 +528,10 @@ impl ThreadRequestProcessor {
         request_id: &ConnectionRequestId,
         params: ThreadUnsubscribeParams,
     ) -> Result<Option<ClientResponsePayload>, JSONRPCErrorError> {
+        let _trace = codex_utils_pty::trace::span(
+            "codex_shutdown",
+            format!("{{\"thread_id\":\"{}\"}}", params.thread_id),
+        );
         self.thread_unsubscribe_response_inner(params, request_id.connection_id)
             .await
             .map(|response| Some(response.into()))
@@ -1245,12 +1249,19 @@ impl ThreadRequestProcessor {
 
     pub(crate) async fn drain_background_tasks(&self) {
         self.background_tasks.close();
-        if tokio::time::timeout(Duration::from_secs(10), self.background_tasks.wait())
+        codex_utils_pty::trace::emit("codex_shutdown", "background_drain_begin", "{}");
+        let timed_out = tokio::time::timeout(Duration::from_secs(10), self.background_tasks.wait())
             .await
-            .is_err()
-        {
+            .is_err();
+        if timed_out {
             warn!("timed out waiting for background tasks to shut down; proceeding");
+            codex_utils_pty::trace::emit("codex_shutdown", "background_drain_timeout", "{}");
         }
+        codex_utils_pty::trace::emit(
+            "codex_shutdown",
+            "background_drain_return",
+            &format!("{{\"timed_out\":{timed_out}}}"),
+        );
     }
 
     pub(crate) async fn clear_all_thread_listeners(&self) {
@@ -1258,16 +1269,28 @@ impl ThreadRequestProcessor {
     }
 
     pub(crate) async fn shutdown_threads(&self) {
+        codex_utils_pty::trace::emit("codex_shutdown", "threads_shutdown_begin", "{}");
         let report = self
             .thread_manager
             .shutdown_all_threads_bounded(Duration::from_secs(10))
             .await;
+        let submit_failed_count = report.submit_failed.len();
+        let timed_out_count = report.timed_out.len();
         for thread_id in report.submit_failed {
             warn!("failed to submit Shutdown to thread {thread_id}");
         }
         for thread_id in report.timed_out {
             warn!("timed out waiting for thread {thread_id} to shut down");
         }
+        codex_utils_pty::trace::emit(
+            "codex_shutdown",
+            "threads_shutdown_return",
+            &format!(
+                "{{\"submit_failed\":{},\"timed_out\":{}}}",
+                submit_failed_count,
+                timed_out_count
+            ),
+        );
     }
 
     async fn request_trace_context(
diff --git a/codex-rs/config/src/config_toml.rs b/codex-rs/config/src/config_toml.rs
index 5f0cc99..a814bc7 100644
--- a/codex-rs/config/src/config_toml.rs
+++ b/codex-rs/config/src/config_toml.rs
@@ -153,6 +153,13 @@ pub struct OrchestratorFeatureToml {
 #[derive(Serialize, Deserialize, Debug, Clone, Default, PartialEq, JsonSchema)]
 #[schemars(deny_unknown_fields)]
 pub struct ConfigToml {
+    /// Trusted MBTX runner argv. Omit to disable MBTX execution backends.
+    pub mbtx_command: Option<Vec<String>>,
+
+    /// Selects the MBTX execution boundary. `transparent` routes the existing
+    /// exec_command tool through MBTX without exposing MoonBit source to the model.
+    pub mbtx_backend: Option<String>,
+
     /// Optional override of model selection.
     pub model: Option<String>,
     /// Review model override used by the `/review` feature.
diff --git a/codex-rs/config/src/loader/mod.rs b/codex-rs/config/src/loader/mod.rs
index 48ea0d3..12514b8 100644
--- a/codex-rs/config/src/loader/mod.rs
+++ b/codex-rs/config/src/loader/mod.rs
@@ -73,6 +73,8 @@ const DEFAULT_PROGRAM_DATA_DIR_WINDOWS: &str = r"C:\ProgramData";
 // These settings are still supported from user, system, managed, and runtime
 // config layers.
 const PROJECT_LOCAL_CONFIG_DENYLIST: &[&str] = &[
+    "mbtx_command",
+    "mbtx_backend",
     "openai_base_url",
     "chatgpt_base_url",
     "apps_mcp_product_sku",
diff --git a/codex-rs/core/config.schema.json b/codex-rs/core/config.schema.json
index 24d7988..0c31296 100644
--- a/codex-rs/core/config.schema.json
+++ b/codex-rs/core/config.schema.json
@@ -6493,6 +6493,17 @@
       "description": "User-level marketplace entries keyed by marketplace name.",
       "type": "object"
     },
+    "mbtx_backend": {
+      "description": "Selects the MBTX execution boundary. `transparent` routes the existing exec_command tool through MBTX without exposing MoonBit source to the model.",
+      "type": "string"
+    },
+    "mbtx_command": {
+      "description": "Trusted MBTX runner argv. Omit to disable MBTX execution backends.",
+      "items": {
+        "type": "string"
+      },
+      "type": "array"
+    },
     "mcp_oauth_callback_port": {
       "description": "Optional fixed port for the local HTTP callback server used during MCP OAuth login. When unset, Codex will bind to an ephemeral port chosen by the OS.",
       "format": "uint16",
diff --git a/codex-rs/core/src/config/config_loader_tests.rs b/codex-rs/core/src/config/config_loader_tests.rs
index bb25c2c..9fb7474 100644
--- a/codex-rs/core/src/config/config_loader_tests.rs
+++ b/codex-rs/core/src/config/config_loader_tests.rs
@@ -3731,6 +3731,8 @@ async fn project_layer_ignores_unsupported_config_keys() -> std::io::Result<()>
         dot_codex.join(CONFIG_TOML_FILE),
         r#"
 model = "project-model"
+mbtx_command = ["/tmp/untrusted-runner"]
+mbtx_backend = "transparent"
 model_instructions_file = "instructions.md"
 openai_base_url = "https://attacker.example/v1"
 chatgpt_base_url = "https://attacker.example/backend-api"
@@ -3796,6 +3798,8 @@ wire_api = "responses"
         .expect("expected project layer");
 
     let ignored_project_config_keys = vec![
+        "mbtx_command",
+        "mbtx_backend",
         "openai_base_url",
         "chatgpt_base_url",
         "apps_mcp_product_sku",
diff --git a/codex-rs/core/src/config/mod.rs b/codex-rs/core/src/config/mod.rs
index dca329a..568fa09 100644
--- a/codex-rs/core/src/config/mod.rs
+++ b/codex-rs/core/src/config/mod.rs
@@ -605,6 +605,13 @@ pub enum ThreadStoreConfig {
 /// Application configuration loaded from disk and merged with overrides.
 #[derive(Debug, Clone, PartialEq)]
 pub struct Config {
+    /// Trusted MBTX runner argv used by the selected execution backend.
+    pub mbtx_command: Option<Vec<String>>,
+
+    /// Optional MBTX execution backend. `transparent` replaces the host process
+    /// launcher behind the existing exec_command tool.
+    pub mbtx_backend: Option<String>,
+
     /// Provenance for how this [`Config`] was derived (merged layers + enforced
     /// requirements).
     pub config_layer_stack: ConfigLayerStack,
@@ -4130,6 +4137,8 @@ impl Config {
         .map_err(std::io::Error::from)?;
         let otel = otel::resolve_config(cfg.otel.unwrap_or_default(), &mut startup_warnings);
         let config = Self {
+            mbtx_command: cfg.mbtx_command,
+            mbtx_backend: cfg.mbtx_backend,
             model,
             service_tier,
             review_model,
diff --git a/codex-rs/core/src/exec_policy.rs b/codex-rs/core/src/exec_policy.rs
index 21e414e..5803763 100644
--- a/codex-rs/core/src/exec_policy.rs
+++ b/codex-rs/core/src/exec_policy.rs
@@ -377,6 +377,15 @@ impl ExecPolicyManager {
             &exec_policy_fallback,
             &match_options,
         );
+        codex_utils_pty::trace::emit(
+            "policy",
+            "decision",
+            &serde_json::json!({
+                "commands": commands, "decision": format!("{:?}", evaluation.decision),
+                "matched_rules": format!("{:?}", evaluation.matched_rules)
+            })
+            .to_string(),
+        );
 
         let requested_amendment = if auto_amendment_allowed {
             derive_requested_execpolicy_amendment_from_prefix_rule(
diff --git a/codex-rs/core/src/tools/handlers/unified_exec/exec_command.rs b/codex-rs/core/src/tools/handlers/unified_exec/exec_command.rs
index 8b97ff0..6c03369 100644
--- a/codex-rs/core/src/tools/handlers/unified_exec/exec_command.rs
+++ b/codex-rs/core/src/tools/handlers/unified_exec/exec_command.rs
@@ -219,6 +219,8 @@ impl ExecCommandHandler {
                 )));
             }
         };
+        let tool_started = std::time::Instant::now();
+        codex_utils_pty::trace::emit("tool", "arguments", &serde_json::json!({"tool":"exec_command", "arguments":serde_json::from_str::<serde_json::Value>(&arguments).ok(), "call_id":context.call_id}).to_string());
         let mut args: ExecCommandArgs = match native_cwd.as_ref() {
             Some(native_cwd) => {
                 // The base path only resolves paths nested in the permissions config types.
@@ -242,8 +244,28 @@ impl ExecCommandHandler {
             &turn_environment.selection.environment_id,
         )
         .await;
-        let shell_mode =
-            shell_mode_for_environment(&turn.unified_exec_shell_mode, environment.as_ref());
+        let transparent_mbtx = match turn.config.mbtx_backend.as_deref() {
+            None => false,
+            Some("transparent") => true,
+            Some(value) => {
+                return Err(FunctionCallError::RespondToModel(format!(
+                    "unsupported mbtx_backend {value:?}; expected transparent or an absent setting"
+                )));
+            }
+        };
+        if transparent_mbtx && environment.is_remote() {
+            return Err(FunctionCallError::RespondToModel(
+                "transparent MBTX backend requires a local execution environment".to_string(),
+            ));
+        }
+        // The zsh-fork optimization has its own launch protocol. Transparent
+        // MBTX wraps the ordinary unified-exec launch after policy checks, so
+        // use the direct shell path for this backend.
+        let shell_mode = if transparent_mbtx {
+            codex_tools::UnifiedExecShellMode::Direct
+        } else {
+            shell_mode_for_environment(&turn.unified_exec_shell_mode, environment.as_ref())
+        };
         // Remote environments may use a different OS and must build commands with their native
         // shell; fall back to the session shell when the environment did not report one.
         let shell = turn_environment
@@ -270,7 +292,6 @@ impl ExecCommandHandler {
                 )));
             }
         }
-        let process_id = manager.allocate_process_id().await;
         let resolved_command = get_command(
             &args,
             shell,
@@ -280,6 +301,27 @@ impl ExecCommandHandler {
         .map_err(FunctionCallError::RespondToModel)?;
         let command = resolved_command.command;
         let shell_type = resolved_command.shell_type;
+        codex_utils_pty::trace::emit("tool", "resolved", &serde_json::json!({"argv":command,"shell_mode":format!("{shell_mode:?}"),"shell_type":format!("{shell_type:?}"),"cwd":cwd.to_string(),"call_id":context.call_id}).to_string());
+        let mbtx_launcher = if transparent_mbtx {
+            let launcher = turn.config.mbtx_command.clone().ok_or_else(|| {
+                FunctionCallError::RespondToModel(
+                    "transparent MBTX backend requires mbtx_command".to_string(),
+                )
+            })?;
+            if launcher.is_empty()
+                || !Path::new(&launcher[0]).is_absolute()
+                || launcher.iter().any(|arg| arg.contains('\0'))
+            {
+                return Err(FunctionCallError::RespondToModel(
+                    "mbtx_command must start with an absolute executable path and contain no NUL"
+                        .to_string(),
+                ));
+            }
+            Some(launcher)
+        } else {
+            None
+        };
+        let process_id = manager.allocate_process_id().await;
         let ExecCommandArgs {
             mut tty,
             yield_time_ms,
@@ -404,6 +446,7 @@ impl ExecCommandHandler {
         emit_unified_exec_tty_metric(&turn.session_telemetry, tty);
         let request = ExecCommandRequest {
             command,
+            mbtx_launcher,
             shell_type,
             hook_command: hook_command.clone(),
             process_id,
@@ -430,20 +473,25 @@ impl ExecCommandHandler {
             None => manager.exec_command(request, &context).await,
         };
         match result {
-            Ok(response) => Ok(boxed_tool_output(response)),
+            Ok(mut response) => {
+                response.wall_time = tool_started.elapsed();
+                codex_utils_pty::trace::emit("tool", "result_ready", &serde_json::json!({"tool":"exec_command","call_id":context.call_id,"process_id":response.process_id,"exit_code":response.exit_code,"output_bytes":response.raw_output.len()}).to_string());
+                Ok(boxed_tool_output(response))
+            }
             Err(UnifiedExecError::SandboxDenied {
                 output,
                 original_token_count,
                 output_omitted_bytes,
                 ..
             }) => {
+                codex_utils_pty::trace::emit("tool", "result_ready", &serde_json::json!({"tool":"exec_command","call_id":context.call_id,"process_id":null,"exit_code":output.exit_code,"sandbox_denied":true}).to_string());
                 let output_text = output.aggregated_output.text;
                 let original_token_count =
                     original_token_count.unwrap_or_else(|| approx_token_count(&output_text));
                 Ok(boxed_tool_output(ExecCommandToolOutput {
                     event_call_id: context.call_id.clone(),
                     chunk_id: generate_chunk_id(),
-                    wall_time: output.duration,
+                    wall_time: tool_started.elapsed(),
                     raw_output: output_text.into_bytes(),
                     truncation_policy: turn.model_info().truncation_policy.into(),
                     max_output_tokens,
diff --git a/codex-rs/core/src/tools/handlers/unified_exec/write_stdin.rs b/codex-rs/core/src/tools/handlers/unified_exec/write_stdin.rs
index 657da46..55e8656 100644
--- a/codex-rs/core/src/tools/handlers/unified_exec/write_stdin.rs
+++ b/codex-rs/core/src/tools/handlers/unified_exec/write_stdin.rs
@@ -78,6 +78,7 @@ impl WriteStdinHandler {
             }
         };
 
+        codex_utils_pty::trace::emit("tool", "arguments", &serde_json::json!({"tool":"write_stdin", "arguments":serde_json::from_str::<serde_json::Value>(&arguments).ok(), "call_id":call_id}).to_string());
         let args: WriteStdinArgs = parse_arguments(&arguments)?;
         let context =
             UnifiedExecContext::new(session.clone(), step_context, cancellation_token, call_id);
@@ -112,6 +113,7 @@ impl WriteStdinHandler {
                 FunctionCallError::RespondToModel(message)
             })?;
 
+        codex_utils_pty::trace::emit("tool", "result_ready", &serde_json::json!({"tool":"write_stdin","process_id":args.session_id,"call_id":context.call_id,"exit_code":response.exit_code,"output_bytes":response.raw_output.len()}).to_string());
         Ok(boxed_tool_output(response))
     }
 }
diff --git a/codex-rs/core/src/tools/orchestrator.rs b/codex-rs/core/src/tools/orchestrator.rs
index 3b37571..0215dc0 100644
--- a/codex-rs/core/src/tools/orchestrator.rs
+++ b/codex-rs/core/src/tools/orchestrator.rs
@@ -170,6 +170,10 @@ impl ToolOrchestrator {
         let requirement = tool.exec_approval_requirement(req).unwrap_or_else(|| {
             default_exec_approval_requirement(approval_policy, &file_system_sandbox_policy)
         });
+        let approval_trace = codex_utils_pty::trace::span(
+            "approval",
+            serde_json::json!({"call_id":tool_ctx.call_id}).to_string(),
+        );
         match &requirement {
             ExecApprovalRequirement::Skip { .. } => {
                 if strict_auto_review {
@@ -229,6 +233,7 @@ impl ToolOrchestrator {
             }
         }
 
+        drop(approval_trace);
         // 2) First attempt under the selected sandbox.
         let unsandboxed_allowed =
             !owner_network_policy && unsandboxed_execution_allowed(&file_system_sandbox_policy);
diff --git a/codex-rs/core/src/tools/runtimes/unified_exec.rs b/codex-rs/core/src/tools/runtimes/unified_exec.rs
index aea36d9..3e9a4eb 100644
--- a/codex-rs/core/src/tools/runtimes/unified_exec.rs
+++ b/codex-rs/core/src/tools/runtimes/unified_exec.rs
@@ -65,6 +65,8 @@ const REMOTE_NETWORK_POLICY_DECISION_MARGIN: Duration = Duration::from_secs(10);
 #[derive(Clone, Debug)]
 pub struct UnifiedExecRequest {
     pub command: Vec<String>,
+    /// Trusted MBTX launcher applied only at the final process launch.
+    pub mbtx_launcher: Option<Vec<String>>,
     pub shell_type: ShellType,
     pub hook_command: String,
     pub process_id: i32,
@@ -144,6 +146,34 @@ fn build_unified_exec_sandbox_command(
     })
 }
 
+/// Prefix a fully prepared command with the trusted transparent MBTX launcher.
+///
+/// This is intentionally applied after approvals, shell policy, and sandbox
+/// transforms have inspected the original command. Keeping the launcher out of
+/// `UnifiedExecRequest::command` preserves the command shown to the user and
+/// used by approval, policy, network, and telemetry paths.
+pub(crate) fn apply_mbtx_launcher(
+    launcher: Option<&[String]>,
+    command: Vec<String>,
+) -> Result<Vec<String>, ToolError> {
+    let Some(launcher) = launcher else {
+        return Ok(command);
+    };
+    if launcher.is_empty()
+        || !std::path::Path::new(&launcher[0]).is_absolute()
+        || launcher.iter().any(|arg| arg.contains('\0'))
+    {
+        return Err(ToolError::Rejected(
+            "mbtx_command must start with an absolute executable path and contain no NUL"
+                .to_string(),
+        ));
+    }
+    let mut launched = launcher.to_vec();
+    launched.extend(["exec".to_string(), "--".to_string()]);
+    launched.extend(command);
+    Ok(launched)
+}
+
 impl<'a> UnifiedExecRuntime<'a> {
     /// Creates a runtime bound to the shared unified-exec process manager.
     pub fn new(manager: &'a UnifiedExecProcessManager, shell_mode: UnifiedExecShellMode) -> Self {
@@ -258,6 +288,10 @@ impl<'a> ToolRuntime<UnifiedExecRequest, UnifiedExecAttempt> for UnifiedExecRunt
         attempt: &SandboxAttempt<'_>,
         ctx: &ToolCtx,
     ) -> Result<UnifiedExecAttempt, ToolError> {
+        let _trace = codex_utils_pty::trace::span(
+            "sandbox_setup_spawn",
+            serde_json::json!({"call_id":ctx.call_id}).to_string(),
+        );
         let base_command = &req.command;
         let windows_sandbox_proxy_settings_mode = ctx.session.windows_sandbox_proxy_settings_mode;
         let session_shell = ctx.session.user_shell();
@@ -286,7 +320,12 @@ impl<'a> ToolRuntime<UnifiedExecRequest, UnifiedExecAttempt> for UnifiedExecRunt
             req.network.as_ref(),
             launch_sandbox_permissions,
         ));
-        let env = exec_env_for_sandbox_permissions(&req.env, launch_sandbox_permissions);
+        let mut env = exec_env_for_sandbox_permissions(&req.env, launch_sandbox_permissions);
+        if req.mbtx_launcher.is_some() {
+            // Codex sends cancellation to the whole process group. Do not deliver
+            // the same signal to the child a second time through the launcher.
+            env.insert("MBTX_CANCEL_SCOPE".into(), "caller-process-group".into());
+        }
         let (mut env, managed_network_context, network_proxy_launch) = match managed_network {
             Some(network) if environment_is_remote => {
                 let mut launch = network.remote_launch_config().await.map_err(|err| {
@@ -413,6 +452,7 @@ impl<'a> ToolRuntime<UnifiedExecRequest, UnifiedExecAttempt> for UnifiedExecRunt
         } else {
             command
         };
+        let command = apply_mbtx_launcher(req.mbtx_launcher.as_deref(), command)?;
         let sidecar_permissions = metrics_sidecar
             .as_ref()
             .map(PluginMetricsSidecar::additional_permissions);
@@ -465,6 +505,7 @@ impl<'a> ToolRuntime<UnifiedExecRequest, UnifiedExecAttempt> for UnifiedExecRunt
                 .await?
             {
                 Some(prepared) => {
+                    codex_utils_pty::trace::emit("tool", "execution_path", &serde_json::json!({"call_id":ctx.call_id,"shell_mode":"ZshFork","process_id":req.process_id}).to_string());
                     if req.turn_environment.environment.is_remote() {
                         return Err(ToolError::Rejected(
                             "unified_exec zsh-fork is not supported for remote environments"
@@ -519,6 +560,7 @@ impl<'a> ToolRuntime<UnifiedExecRequest, UnifiedExecAttempt> for UnifiedExecRunt
             error @ ToolError::Codex(_) => error,
         })?;
         let options = unified_exec_options(attempt.network_denial_cancellation_token.clone());
+        codex_utils_pty::trace::emit("tool", "execution_path", &serde_json::json!({"call_id":ctx.call_id,"shell_mode":"Direct","process_id":req.process_id,"argv":command.args,"program":command.program}).to_string());
         let process = self
             .manager
             .open_session_with_exec_env(
@@ -617,6 +659,45 @@ mod tests {
         }
     }
 
+    #[test]
+    fn transparent_launcher_is_added_after_the_original_command() {
+        let original = vec![
+            "/bin/zsh".to_string(),
+            "-lc".to_string(),
+            "printf '%s' '$(touch SHOULD_NOT_EXIST)'".to_string(),
+        ];
+        assert_eq!(
+            apply_mbtx_launcher(None, original.clone()).expect("no launcher preserves command"),
+            original
+        );
+        assert_eq!(
+            apply_mbtx_launcher(Some(&["/opt/mbtx".to_string()]), original.clone())
+                .expect("trusted launcher prefixes command"),
+            vec![
+                "/opt/mbtx".to_string(),
+                "exec".to_string(),
+                "--".to_string(),
+                "/bin/zsh".to_string(),
+                "-lc".to_string(),
+                "printf '%s' '$(touch SHOULD_NOT_EXIST)'".to_string(),
+            ]
+        );
+    }
+
+    #[test]
+    fn transparent_launcher_rejects_untrusted_shape() {
+        for launcher in [
+            Vec::new(),
+            vec!["mbtx".to_string()],
+            vec!["/opt/mbtx\0bad".to_string()],
+        ] {
+            assert!(matches!(
+                apply_mbtx_launcher(Some(&launcher), vec!["echo".to_string()]),
+                Err(ToolError::Rejected(message)) if message.contains("mbtx_command")
+            ));
+        }
+    }
+
     #[tokio::test]
     async fn approval_key_includes_environment_id() {
         let manager = UnifiedExecProcessManager::default();
@@ -654,6 +735,7 @@ mod tests {
         let runtime = UnifiedExecRuntime::new(&manager, UnifiedExecShellMode::Direct);
         let request = UnifiedExecRequest {
             command: vec!["pwd".to_string()],
+            mbtx_launcher: None,
             shell_type: ShellType::Sh,
             hook_command: "pwd".to_string(),
             process_id: 1000,
@@ -757,6 +839,7 @@ mod tests {
             .expect("current dir is absolute");
         UnifiedExecRequest {
             command: vec!["zsh".to_string(), "-c".to_string(), "echo hi".to_string()],
+            mbtx_launcher: None,
             shell_type: ShellType::Zsh,
             hook_command: "echo hi".to_string(),
             process_id: 1000,
diff --git a/codex-rs/core/src/unified_exec/async_watcher.rs b/codex-rs/core/src/unified_exec/async_watcher.rs
index 366f631..3baa109 100644
--- a/codex-rs/core/src/unified_exec/async_watcher.rs
+++ b/codex-rs/core/src/unified_exec/async_watcher.rs
@@ -30,7 +30,7 @@ use codex_protocol::protocol::ExecCommandSource;
 use codex_protocol::protocol::ExecOutputStream;
 use codex_utils_path_uri::PathUri;
 
-pub(crate) const TRAILING_OUTPUT_GRACE: Duration = Duration::from_millis(100);
+pub(crate) const TRAILING_OUTPUT_GRACE: Duration = Duration::from_millis(500);
 
 /// Upper bound for a single ExecCommandOutputDelta chunk emitted by unified exec.
 ///
@@ -56,12 +56,13 @@ struct Buffer<const MAX_BYTES: usize = UNIFIED_EXEC_OUTPUT_DELTA_MAX_BYTES> {
 /// Spawn a background task that continuously reads from the PTY, appends to the
 /// shared transcript, and emits ExecCommandOutputDelta events on UTF‑8
 /// boundaries.
-pub(crate) fn start_streaming_output(
+pub(crate) async fn start_streaming_output(
     process: &UnifiedExecProcess,
     context: &UnifiedExecContext,
     transcript: Arc<Mutex<HeadTailBuffer>>,
 ) {
-    let mut receiver = process.output_receiver();
+    let (initial, mut receiver) = process.output_receiver().await;
+    *transcript.lock().await = initial;
     let output_drained = process.output_drained_notify();
     let exit_token = process.cancellation_token();
     let OutputHandles {
diff --git a/codex-rs/core/src/unified_exec/head_tail_buffer.rs b/codex-rs/core/src/unified_exec/head_tail_buffer.rs
index 0c9df7c..afe3713 100644
--- a/codex-rs/core/src/unified_exec/head_tail_buffer.rs
+++ b/codex-rs/core/src/unified_exec/head_tail_buffer.rs
@@ -6,7 +6,7 @@ use std::collections::VecDeque;
 /// dropping the middle once it exceeds the configured maximum. The buffer is
 /// symmetric meaning 50% of the capacity is allocated to the head and 50% is
 /// allocated to the tail.
-#[derive(Debug, Default)]
+#[derive(Clone, Debug, Default)]
 #[cfg_attr(test, derive(Eq, PartialEq))]
 pub(crate) struct HeadTailBuffer<const MAX_BYTES: usize = UNIFIED_EXEC_OUTPUT_MAX_BYTES> {
     head: Vec<u8>,
diff --git a/codex-rs/core/src/unified_exec/mod.rs b/codex-rs/core/src/unified_exec/mod.rs
index 8dc8f35..701da1f 100644
--- a/codex-rs/core/src/unified_exec/mod.rs
+++ b/codex-rs/core/src/unified_exec/mod.rs
@@ -107,6 +107,9 @@ impl UnifiedExecContext {
 #[derive(Debug)]
 pub(crate) struct ExecCommandRequest {
     pub command: Vec<String>,
+    /// Trusted MBTX launcher applied only after approval and sandbox planning.
+    /// The user-visible command remains in `command` for policy and events.
+    pub mbtx_launcher: Option<Vec<String>>,
     pub shell_type: ShellType,
     pub hook_command: String,
     pub process_id: i32,
diff --git a/codex-rs/core/src/unified_exec/process.rs b/codex-rs/core/src/unified_exec/process.rs
index 81946e4..392c429 100644
--- a/codex-rs/core/src/unified_exec/process.rs
+++ b/codex-rs/core/src/unified_exec/process.rs
@@ -172,8 +172,13 @@ impl UnifiedExecProcess {
         &self.output
     }
 
-    pub(super) fn output_receiver(&self) -> tokio::sync::broadcast::Receiver<Vec<u8>> {
-        self.output_tx.subscribe()
+    pub(super) async fn output_receiver(
+        &self,
+    ) -> (HeadTailBuffer, tokio::sync::broadcast::Receiver<Vec<u8>>) {
+        // Snapshot and subscribe under the producer lock so early bytes are
+        // neither lost nor delivered twice to the event transcript.
+        let buffer = self.output.output_buffer.lock().await;
+        (buffer.clone(), self.output_tx.subscribe())
     }
 
     pub(super) fn cancellation_token(&self) -> CancellationToken {
@@ -515,8 +520,8 @@ impl UnifiedExecProcess {
                         let bytes = chunk.chunk.into_inner();
                         let mut guard = output_buffer.lock().await;
                         guard.push_chunk(&bytes);
-                        drop(guard);
                         let _ = output_tx.send(bytes);
+                        drop(guard);
                         output_notify.notify_waiters();
                     }
                     last_seq = last_seq.max(next_seq.saturating_sub(1));
@@ -558,8 +563,8 @@ impl UnifiedExecProcess {
                         let bytes = chunk.chunk.into_inner();
                         let mut guard = output_buffer.lock().await;
                         guard.push_chunk(&bytes);
-                        drop(guard);
                         let _ = output_tx.send(bytes);
+                        drop(guard);
                         output_notify.notify_waiters();
                     }
                     ExecProcessEvent::Exited {
@@ -619,8 +624,8 @@ impl UnifiedExecProcess {
                     Ok(chunk) => {
                         let mut guard = output_buffer.lock().await;
                         guard.push_chunk(&chunk);
-                        drop(guard);
                         let _ = output_tx.send(chunk);
+                        drop(guard);
                         output_notify.notify_waiters();
                     }
                     Err(tokio::sync::broadcast::error::RecvError::Lagged(_)) => continue,
diff --git a/codex-rs/core/src/unified_exec/process_manager.rs b/codex-rs/core/src/unified_exec/process_manager.rs
index 2121857..7e3ab84 100644
--- a/codex-rs/core/src/unified_exec/process_manager.rs
+++ b/codex-rs/core/src/unified_exec/process_manager.rs
@@ -490,10 +490,21 @@ impl UnifiedExecProcessManager {
         mut completion: Option<&mut Completion<'_>>,
     ) -> Result<ExecCommandToolOutput, UnifiedExecError> {
         let cwd = request.cwd.clone();
+        codex_utils_pty::trace::emit(
+            "policy_sandbox",
+            "begin",
+            &serde_json::json!({"call_id":context.call_id}).to_string(),
+        );
         let process = self
             .open_session_with_sandbox(&request, cwd.clone(), context)
             .await;
 
+        codex_utils_pty::trace::emit(
+            "policy_sandbox",
+            "return",
+            &serde_json::json!({"call_id":context.call_id,"success":process.is_ok()}).to_string(),
+        );
+
         let (attempt, mut deferred_network_approval) = match process {
             Ok((attempt, deferred_network_approval)) => (attempt, deferred_network_approval),
             Err(err) => {
@@ -553,7 +564,7 @@ impl UnifiedExecProcessManager {
         );
         emitter.emit(event_ctx, ToolEventStage::Begin).await;
 
-        start_streaming_output(&process, context, Arc::clone(&transcript));
+        start_streaming_output(&process, context, Arc::clone(&transcript)).await;
         let start = Instant::now();
         // Persist live sessions before the initial yield wait so interrupting the
         // turn cannot drop the last Arc and terminate the background process.
@@ -617,6 +628,8 @@ impl UnifiedExecProcessManager {
             }
         }
         let wall_time = Instant::now().saturating_duration_since(start);
+        codex_utils_pty::trace::emit("tool", "process_ready", &serde_json::json!({"tool":"exec_command","call_id":context.call_id,"process_id":request.process_id}).to_string());
+        codex_utils_pty::trace::resources(&context.call_id);
 
         let original_token_count = usize::try_from(approx_tokens_from_byte_count(
             collected_output.total_bytes(),
@@ -1361,6 +1374,10 @@ impl UnifiedExecProcessManager {
         strip_output_env(&mut env);
         let mut explicit_env_overrides = shell_environment_policy.r#set.clone();
         strip_output_env(&mut explicit_env_overrides);
+        if codex_utils_pty::trace::enabled() {
+            env.insert("MBTX_TOOL_CALL_ID".into(), context.call_id.clone());
+            explicit_env_overrides.insert("MBTX_TOOL_CALL_ID".into(), context.call_id.clone());
+        }
         let exec_server_env_config = ExecServerEnvConfig {
             policy: exec_env_policy_from_shell_policy(shell_environment_policy),
             local_policy_env,
@@ -1407,6 +1424,7 @@ impl UnifiedExecProcessManager {
             .await;
         let req = UnifiedExecToolRequest {
             command: request.command.clone(),
+            mbtx_launcher: request.mbtx_launcher.clone(),
             shell_type: request.shell_type,
             hook_command: request.hook_command.clone(),
             process_id: request.process_id,
@@ -1460,7 +1478,7 @@ impl UnifiedExecProcessManager {
         mut pause_state: Option<watch::Receiver<bool>>,
         mut deadline: Instant,
     ) -> HeadTailBuffer<MAX_BYTES> {
-        const POST_EXIT_CLOSE_WAIT_CAP: Duration = Duration::from_millis(50);
+        const POST_EXIT_CLOSE_WAIT_CAP: Duration = Duration::from_millis(500);
 
         let OutputHandles {
             output_buffer,
@@ -1509,6 +1527,7 @@ impl UnifiedExecProcessManager {
                         .get_or_insert_with(|| now + remaining.min(POST_EXIT_CLOSE_WAIT_CAP));
                     let close_wait_remaining = close_wait_deadline.saturating_duration_since(now);
                     if close_wait_remaining == Duration::ZERO {
+                        codex_utils_pty::trace::emit("io", "drain_censored", "{}");
                         break;
                     }
                     let notified = wait_for_output.unwrap_or_else(|| output_notify.notified());
@@ -1518,7 +1537,10 @@ impl UnifiedExecProcessManager {
                     tokio::select! {
                         _ = &mut notified => {}
                         _ = &mut closed => {}
-                        _ = tokio::time::sleep(close_wait_remaining) => break,
+                        _ = tokio::time::sleep(close_wait_remaining) => {
+                            codex_utils_pty::trace::emit("io", "drain_censored", "{}");
+                            break;
+                        },
                         _ = Self::wait_for_pause_change(pause_state.as_ref()) => {}
                     }
                     continue;
diff --git a/codex-rs/core/src/unified_exec/process_manager_tests.rs b/codex-rs/core/src/unified_exec/process_manager_tests.rs
index 523e7eb..eb74a4c 100644
--- a/codex-rs/core/src/unified_exec/process_manager_tests.rs
+++ b/codex-rs/core/src/unified_exec/process_manager_tests.rs
@@ -424,6 +424,7 @@ async fn failed_initial_end_for_unstored_process_uses_fallback_output() {
             "-lc".to_string(),
             "echo before".to_string(),
         ],
+        mbtx_launcher: None,
         shell_type: crate::shell::ShellType::Sh,
         hook_command: "echo before".to_string(),
         process_id: 123,
diff --git a/codex-rs/exec/Cargo.toml b/codex-rs/exec/Cargo.toml
index 83da982..cb8bc6b 100644
--- a/codex-rs/exec/Cargo.toml
+++ b/codex-rs/exec/Cargo.toml
@@ -41,6 +41,7 @@ codex-rollout = { workspace = true }
 codex-utils-absolute-path = { workspace = true }
 codex-utils-cli = { workspace = true }
 codex-utils-oss = { workspace = true }
+codex-utils-pty = { workspace = true }
 codex-utils-sandbox-summary = { workspace = true }
 owo-colors = { workspace = true }
 serde = { workspace = true, features = ["derive"] }
diff --git a/codex-rs/exec/src/lib.rs b/codex-rs/exec/src/lib.rs
index 68cde23..018ebe8 100644
--- a/codex-rs/exec/src/lib.rs
+++ b/codex-rs/exec/src/lib.rs
@@ -1112,6 +1112,11 @@ async fn run_exec_session(args: ExecRunArgs) -> anyhow::Result<()> {
                     match event_processor.process_server_notification(notification) {
                         CodexStatus::Running => {}
                         CodexStatus::InitiateShutdown => {
+                            codex_utils_pty::trace::emit(
+                                "codex_shutdown",
+                                "unsubscribe_begin",
+                                "{}",
+                            );
                             if let Err(err) = request_shutdown(
                                 &client,
                                 &mut request_ids,
@@ -1121,6 +1126,11 @@ async fn run_exec_session(args: ExecRunArgs) -> anyhow::Result<()> {
                             {
                                 warn!("thread/unsubscribe failed during shutdown: {err}");
                             }
+                            codex_utils_pty::trace::emit(
+                                "codex_shutdown",
+                                "unsubscribe_return",
+                                "{}",
+                            );
                             break;
                         }
                     }
@@ -1134,9 +1144,11 @@ async fn run_exec_session(args: ExecRunArgs) -> anyhow::Result<()> {
         }
     }
 
+    codex_utils_pty::trace::emit("codex_shutdown", "client_shutdown_begin", "{}");
     if let Err(err) = client.shutdown().await {
         warn!("in-process app-server shutdown failed: {err}");
     }
+    codex_utils_pty::trace::emit("codex_shutdown", "client_shutdown_return", "{}");
     event_processor.print_final_output();
     if error_seen {
         std::process::exit(1);
diff --git a/codex-rs/responses-api-proxy/Cargo.toml b/codex-rs/responses-api-proxy/Cargo.toml
index 05d6388..fa94702 100644
--- a/codex-rs/responses-api-proxy/Cargo.toml
+++ b/codex-rs/responses-api-proxy/Cargo.toml
@@ -22,6 +22,7 @@ clap = { workspace = true, features = ["derive"] }
 codex-process-hardening = { workspace = true }
 ctor = { workspace = true }
 libc = { workspace = true }
+httpdate = { workspace = true }
 reqwest = { workspace = true, features = ["blocking", "json", "rustls-tls"] }
 serde = { workspace = true, features = ["derive"] }
 serde_json = { workspace = true }
diff --git a/codex-rs/responses-api-proxy/src/lib.rs b/codex-rs/responses-api-proxy/src/lib.rs
index 307fa05..14574e8 100644
--- a/codex-rs/responses-api-proxy/src/lib.rs
+++ b/codex-rs/responses-api-proxy/src/lib.rs
@@ -6,8 +6,8 @@ use std::net::SocketAddr;
 use std::net::TcpListener;
 use std::path::Path;
 use std::path::PathBuf;
-use std::sync::Arc;
-use std::time::Duration;
+use std::sync::{Arc, Mutex};
+use std::time::{Duration, Instant, SystemTime};
 
 use anyhow::Context;
 use anyhow::Result;
@@ -29,6 +29,7 @@ use tiny_http::Server;
 use tiny_http::StatusCode;
 
 mod dump;
+mod observation;
 mod read_api_key;
 use dump::ExchangeDumper;
 use read_api_key::read_auth_header_from_stdin;
@@ -56,6 +57,14 @@ pub struct Args {
     /// Directory where request/response dumps should be written as JSON.
     #[arg(long, value_name = "DIR")]
     pub dump_dir: Option<PathBuf>,
+
+    /// Minimum interval between upstream request starts.
+    #[arg(long, default_value_t = 0)]
+    pub min_request_interval_ms: u64,
+
+    /// Timeout for a complete upstream response, including its stream. Zero disables it.
+    #[arg(long, default_value_t = 0)]
+    pub request_timeout_ms: u64,
 }
 
 #[derive(Serialize)]
@@ -86,6 +95,11 @@ pub fn run_main(args: Args) -> Result<()> {
         upstream_url,
         host_header,
     });
+    let observer = args
+        .dump_dir
+        .as_ref()
+        .map(|directory| observation::Observer::new(directory.clone()).map(Arc::new))
+        .transpose()?;
     let dump_dir = args
         .dump_dir
         .map(ExchangeDumper::new)
@@ -102,10 +116,14 @@ pub fn run_main(args: Args) -> Result<()> {
     let client = Arc::new(
         Client::builder()
             // Disable reqwest's 30s default so long-lived response streams keep flowing.
-            .timeout(None::<Duration>)
+            .timeout(
+                (args.request_timeout_ms > 0)
+                    .then(|| Duration::from_millis(args.request_timeout_ms)),
+            )
             .build()
             .context("building reqwest client")?,
     );
+    let request_gate = Arc::new(Mutex::new(Instant::now()));
 
     eprintln!("responses-api-proxy listening on {bound_addr}");
 
@@ -114,6 +132,9 @@ pub fn run_main(args: Args) -> Result<()> {
         let client = client.clone();
         let forward_config = forward_config.clone();
         let dump_dir = dump_dir.clone();
+        let request_gate = request_gate.clone();
+        let request_interval = Duration::from_millis(args.min_request_interval_ms);
+        let observer = observer.clone();
         std::thread::spawn(move || {
             if http_shutdown && request.method() == &Method::Get && request.url() == "/shutdown" {
                 let _ = request.respond(Response::new_empty(StatusCode(200)));
@@ -126,6 +147,9 @@ pub fn run_main(args: Args) -> Result<()> {
                 &forward_config,
                 dump_dir.as_deref(),
                 request,
+                &request_gate,
+                request_interval,
+                observer.as_ref(),
             ) {
                 eprintln!("forwarding error: {e}");
             }
@@ -166,6 +190,9 @@ fn forward_request(
     config: &ForwardConfig,
     dump_dir: Option<&ExchangeDumper>,
     mut req: Request,
+    request_gate: &Mutex<Instant>,
+    request_interval: Duration,
+    observer: Option<&Arc<observation::Observer>>,
 ) -> Result<()> {
     // Only allow POST /v1/responses exactly, no query string.
     let method = req.method().clone();
@@ -182,6 +209,10 @@ fn forward_request(
     let mut body = Vec::new();
     let reader = req.as_reader();
     reader.read_to_end(&mut body)?;
+    let trace = observer.map(|observer| Arc::new(observer.request()));
+    if let Some(trace) = &trace {
+        trace.emit("request_received", serde_json::json!({"bytes":body.len()}));
+    }
 
     let exchange_dump = dump_dir.and_then(|dump_dir| {
         dump_dir
@@ -220,12 +251,75 @@ fn forward_request(
 
     headers.insert(HOST, config.host_header.clone());
 
+    if let Some(trace) = &trace {
+        trace.emit("gate_queued", serde_json::json!({}));
+    }
+    let mut gate = request_gate
+        .lock()
+        .map_err(|_| anyhow!("request gate poisoned"))?;
+    if let Some(trace) = &trace {
+        trace.emit("gate_acquired", serde_json::json!({}));
+    }
+    let next = *gate;
+    let now = Instant::now();
+    if now < next {
+        if let Some(trace) = &trace {
+            trace.emit("rate_wait_begin", serde_json::json!({}));
+        }
+        std::thread::sleep(next - now);
+        if let Some(trace) = &trace {
+            trace.emit("rate_wait_end", serde_json::json!({}));
+        }
+    }
+    *gate = Instant::now() + request_interval;
+    if let Some(trace) = &trace {
+        trace.emit("request_send", serde_json::json!({}));
+    }
     let upstream_resp = client
         .post(config.upstream_url.clone())
         .headers(headers)
         .body(body)
-        .send()
-        .context("forwarding request to upstream")?;
+        .send();
+    let upstream_resp = match upstream_resp {
+        Ok(response) => response,
+        Err(error) => {
+            if let Some(trace) = &trace {
+                trace.emit(
+                    "transport_error",
+                    serde_json::json!({"error":error.to_string()}),
+                );
+            }
+            return Err(error).context("forwarding request to upstream");
+        }
+    };
+    let retry_after = upstream_resp
+        .headers()
+        .get("retry-after")
+        .and_then(|value| value.to_str().ok());
+    if upstream_resp.status().as_u16() == 429 {
+        let wait = retry_after
+            .and_then(|value| {
+                value
+                    .parse::<u64>()
+                    .ok()
+                    .map(Duration::from_secs)
+                    .or_else(|| {
+                        httpdate::parse_http_date(value).ok().map(|deadline| {
+                            deadline
+                                .duration_since(SystemTime::now())
+                                .unwrap_or_default()
+                        })
+                    })
+            })
+            .unwrap_or(Duration::from_secs(30));
+        *gate = (*gate).max(Instant::now() + wait);
+    }
+    if let Some(trace) = &trace {
+        trace.emit(
+            "response_headers",
+            serde_json::json!({"status":upstream_resp.status().as_u16(),"retry_after":retry_after}),
+        );
+    }
 
     // We have to create an adapter between a `reqwest::blocking::Response`
     // and a `tiny_http::Response`. Fortunately, `reqwest::blocking::Response`
@@ -255,13 +349,22 @@ fn forward_request(
         }
     });
 
+    let headers = upstream_resp.headers().clone();
+    let observed_body: Box<dyn Read + Send> = match trace.clone() {
+        Some(trace) => Box::new(observation::ObservedBody {
+            body: upstream_resp,
+            trace,
+            first: false,
+            finished: false,
+            pending: Vec::new(),
+        }),
+        None => Box::new(upstream_resp),
+    };
     let response_body: Box<dyn Read + Send> = if let Some(exchange_dump) = exchange_dump {
-        let headers = upstream_resp.headers().clone();
-        Box::new(exchange_dump.tee_response_body(status.as_u16(), &headers, upstream_resp))
+        Box::new(exchange_dump.tee_response_body(status.as_u16(), &headers, observed_body))
     } else {
-        Box::new(upstream_resp)
+        observed_body
     };
-
     let response = Response::new(
         StatusCode(status.as_u16()),
         response_headers,
@@ -270,6 +373,12 @@ fn forward_request(
         None,
     );
 
-    let _ = req.respond(response);
+    let forwarded = req.respond(response);
+    if let Some(trace) = &trace {
+        trace.emit(
+            "request_finished",
+            serde_json::json!({"forwarded":forwarded.is_ok()}),
+        );
+    }
     Ok(())
 }
diff --git a/codex-rs/shell-command/src/bash.rs b/codex-rs/shell-command/src/bash.rs
index ddd5807..074a6bb 100644
--- a/codex-rs/shell-command/src/bash.rs
+++ b/codex-rs/shell-command/src/bash.rs
@@ -169,10 +169,7 @@ fn parse_plain_command_from_node(cmd: tree_sitter::Node, src: &str) -> Option<Ve
         match child.kind() {
             "command_name" => {
                 let word_node = child.named_child(0)?;
-                if word_node.kind() != "word" {
-                    return None;
-                }
-                words.push(word_node.utf8_text(src.as_bytes()).ok()?.to_owned());
+                words.push(parse_literal_shell_word(word_node, src)?);
             }
             "word" | "number" => {
                 words.push(child.utf8_text(src.as_bytes()).ok()?.to_owned());
@@ -327,6 +324,21 @@ mod tests {
         assert_eq!(cmds, vec![vec!["ls".to_string(), "-1".to_string()]]);
     }
 
+    #[test]
+    fn quoted_programs_keep_literal_identity_for_policy() {
+        for script in [
+            "'/path with space/tool' arg",
+            "\"/path with space/tool\" arg",
+        ] {
+            assert_eq!(
+                parse_seq(script),
+                Some(vec![vec!["/path with space/tool".into(), "arg".into()]])
+            );
+        }
+        assert_eq!(parse_seq("\"$TOOL\" arg"), None);
+        assert_eq!(parse_seq("\"$(tool)\" arg"), None);
+    }
+
     #[test]
     fn accepts_multiple_commands_with_allowed_operators() {
         let src = "ls && pwd; echo 'hi there' | wc -l";
diff --git a/codex-rs/utils/pty/src/lib.rs b/codex-rs/utils/pty/src/lib.rs
index 1200c11..6852bb0 100644
--- a/codex-rs/utils/pty/src/lib.rs
+++ b/codex-rs/utils/pty/src/lib.rs
@@ -4,6 +4,7 @@ pub mod process_group;
 pub mod pty;
 #[cfg(test)]
 mod tests;
+pub mod trace;
 #[cfg(unix)]
 mod unix_io;
 #[cfg(windows)]
diff --git a/codex-rs/utils/pty/src/pipe.rs b/codex-rs/utils/pty/src/pipe.rs
index e189c13..1ba5f2f 100644
--- a/codex-rs/utils/pty/src/pipe.rs
+++ b/codex-rs/utils/pty/src/pipe.rs
@@ -106,19 +106,37 @@ fn kill_process(pid: u32) -> io::Result<()> {
     }
 }
 
-async fn read_output_stream<R>(mut reader: R, output_tx: mpsc::Sender<Vec<u8>>)
-where
+async fn read_output_stream<R>(
+    mut reader: R,
+    output_tx: mpsc::Sender<Vec<u8>>,
+    trace_id: u64,
+    stream: &str,
+) where
     R: AsyncRead + Unpin,
 {
     let mut buf = vec![0u8; 8_192];
     loop {
         match reader.read(&mut buf).await {
-            Ok(0) => break,
+            Ok(0) => {
+                crate::trace::stream(trace_id, stream, &[]);
+                break;
+            }
             Ok(n) => {
+                crate::trace::stream(trace_id, stream, &buf[..n]);
                 let _ = output_tx.send(buf[..n].to_vec()).await;
             }
             Err(ref e) if e.kind() == ErrorKind::Interrupted => continue,
-            Err(_) => break,
+            Err(error) => {
+                crate::trace::emit(
+                    "io",
+                    "read_error",
+                    &format!(
+                        "{{\"stream_id\":{trace_id},\"errno\":{}}}",
+                        error.raw_os_error().unwrap_or(-1)
+                    ),
+                );
+                break;
+            }
         }
     }
 }
@@ -189,7 +207,10 @@ async fn spawn_process_with_stdin_mode(
 
     #[cfg(windows)]
     let job = crate::win::JobObject::create().map(Arc::new);
+    let trace_id = crate::trace::next_id();
+    crate::trace::spawn_begin(trace_id, false, env);
     let mut child = command.spawn()?;
+    crate::trace::spawn_return(trace_id, child.id(), env);
     #[cfg(windows)]
     let windows_terminator = {
         // Accept the small race: a descendant created between spawn and
@@ -242,13 +263,13 @@ async fn spawn_process_with_stdin_mode(
     let stdout_handle = stdout.map(|stdout| {
         let stdout_tx = stdout_tx.clone();
         tokio::spawn(async move {
-            read_output_stream(BufReader::new(stdout), stdout_tx).await;
+            read_output_stream(BufReader::new(stdout), stdout_tx, trace_id, "stdout").await;
         })
     });
     let stderr_handle = stderr.map(|stderr| {
         let stderr_tx = stderr_tx.clone();
         tokio::spawn(async move {
-            read_output_stream(BufReader::new(stderr), stderr_tx).await;
+            read_output_stream(BufReader::new(stderr), stderr_tx, trace_id, "stderr").await;
         })
     });
     let mut reader_abort_handles = Vec::new();
@@ -280,6 +301,19 @@ async fn spawn_process_with_stdin_mode(
     let wait_handle: JoinHandle<()> = tokio::spawn(async move {
         let code = match child.wait().await {
             Ok(status) => {
+                #[cfg(unix)]
+                {
+                    use std::os::unix::process::ExitStatusExt;
+                    crate::trace::emit(
+                        "child",
+                        "wait_return",
+                        &format!(
+                            "{{\"stream_id\":{trace_id},\"exit_code\":{},\"signal\":{}}}",
+                            status.code().map_or("null".into(), |x| x.to_string()),
+                            status.signal().map_or("null".into(), |x| x.to_string())
+                        ),
+                    );
+                }
                 #[cfg(windows)]
                 if let Some(job) = wait_job
                     && let Err(err) = job.preserve_descendants()
diff --git a/codex-rs/utils/pty/src/process.rs b/codex-rs/utils/pty/src/process.rs
index 5593450..d445853 100644
--- a/codex-rs/utils/pty/src/process.rs
+++ b/codex-rs/utils/pty/src/process.rs
@@ -8,6 +8,7 @@ use std::sync::Mutex as StdMutex;
 use std::sync::atomic::AtomicBool;
 
 use anyhow::anyhow;
+#[cfg(not(unix))]
 use portable_pty::MasterPty;
 use portable_pty::PtySize;
 use portable_pty::SlavePty;
@@ -84,6 +85,7 @@ pub(crate) trait PtyHandleKeepAlive: Send {}
 impl<T: Send + ?Sized> PtyHandleKeepAlive for T {}
 
 pub(crate) enum PtyMasterHandle {
+    #[cfg(not(unix))]
     Resizable(Box<dyn MasterPty + Send>),
     #[cfg(unix)]
     Opaque {
@@ -191,6 +193,7 @@ impl ProcessHandle {
                 .map_err(|_| anyhow!("failed to lock PTY handles"))?;
             if let Some(handles) = handles.as_ref() {
                 return match &handles._master {
+                    #[cfg(not(unix))]
                     PtyMasterHandle::Resizable(master) => master.resize(size.into()),
                     #[cfg(unix)]
                     PtyMasterHandle::Opaque { raw_fd, .. } => resize_raw_pty(*raw_fd, size),
@@ -219,6 +222,7 @@ impl ProcessHandle {
     /// Attempts to kill the child while leaving the reader/writer tasks alive
     /// so callers can still drain output until EOF.
     pub fn request_terminate(&self) {
+        crate::trace::emit("cancellation", "request_terminate", "{}");
         if let Ok(mut killer_opt) = self.killer.lock()
             && let Some(mut killer) = killer_opt.take()
         {
@@ -227,6 +231,7 @@ impl ProcessHandle {
     }
 
     pub fn signal(&self, signal: ProcessSignal) -> io::Result<()> {
+        crate::trace::emit("cancellation", "interrupt", "{}");
         let Ok(mut killer_opt) = self.killer.lock() else {
             return Ok(());
         };
@@ -244,6 +249,7 @@ impl ProcessHandle {
 
     /// Attempts to kill the child and abort I/O helper tasks.
     pub fn terminate(&self) {
+        crate::trace::emit("cancellation", "terminate", "{}");
         self.request_terminate();
 
         if let Ok(mut h) = self.reader_handle.lock()
@@ -330,6 +336,7 @@ pub fn combine_output_receivers(
             tokio::select! {
                 stdout = stdout_rx.recv(), if stdout_open => match stdout {
                     Some(chunk) => {
+                        crate::trace::emit("io", "merge", &format!("{{\"stream\":\"stdout\",\"bytes\":{}}}", chunk.len()));
                         let _ = combined_tx.send(chunk);
                     }
                     None => {
@@ -338,6 +345,7 @@ pub fn combine_output_receivers(
                 },
                 stderr = stderr_rx.recv(), if stderr_open => match stderr {
                     Some(chunk) => {
+                        crate::trace::emit("io", "merge", &format!("{{\"stream\":\"stderr\",\"bytes\":{}}}", chunk.len()));
                         let _ = combined_tx.send(chunk);
                     }
                     None => {
diff --git a/codex-rs/utils/pty/src/pty.rs b/codex-rs/utils/pty/src/pty.rs
index 1b848b3..ab0b307 100644
--- a/codex-rs/utils/pty/src/pty.rs
+++ b/codex-rs/utils/pty/src/pty.rs
@@ -1,6 +1,7 @@
 use std::collections::HashMap;
 #[cfg(unix)]
 use std::fs::File;
+#[cfg(not(unix))]
 use std::io::ErrorKind;
 #[cfg(unix)]
 use std::os::fd::AsRawFd;
@@ -22,8 +23,9 @@ use std::sync::atomic::AtomicBool;
 use std::time::Duration;
 
 use anyhow::Result;
+#[cfg(not(unix))]
 use portable_pty::CommandBuilder;
-#[cfg(not(windows))]
+#[cfg(all(not(unix), not(windows)))]
 use portable_pty::native_pty_system;
 use tokio::sync::mpsc;
 use tokio::sync::oneshot;
@@ -51,12 +53,14 @@ pub fn conpty_supported() -> bool {
     true
 }
 
+#[cfg(not(unix))]
 struct PtyChildTerminator {
     killer: Box<dyn portable_pty::ChildKiller + Send + Sync>,
     #[cfg(unix)]
     process_group_id: Option<u32>,
 }
 
+#[cfg(not(unix))]
 impl ChildTerminator for PtyChildTerminator {
     fn signal(&mut self, signal: ProcessSignal) -> std::io::Result<()> {
         match signal {
@@ -111,6 +115,7 @@ impl ChildTerminator for RawPidTerminator {
     }
 }
 
+#[cfg(not(unix))]
 fn platform_native_pty_system() -> Box<dyn portable_pty::PtySystem + Send> {
     #[cfg(windows)]
     {
@@ -142,14 +147,18 @@ pub async fn spawn_process(
     let _ = inherited_fds;
 
     #[cfg(unix)]
-    if !inherited_fds.is_empty() {
-        return spawn_process_preserving_fds(program, args, cwd, env, arg0, size, inherited_fds)
-            .await;
+    {
+        // portable-pty reduces signal exits to code 1. The native wait status
+        // preserves signal numbers and also handles an empty inherited-FD list.
+        spawn_process_preserving_fds(program, args, cwd, env, arg0, size, inherited_fds).await
+    }
+    #[cfg(not(unix))]
+    {
+        spawn_process_portable(program, args, cwd, env, arg0, size).await
     }
-
-    spawn_process_portable(program, args, cwd, env, arg0, size).await
 }
 
+#[cfg(not(unix))]
 async fn spawn_process_portable(
     program: &str,
     args: &[String],
@@ -177,7 +186,10 @@ async fn spawn_process_portable(
         command_builder.env(key, value);
     }
 
+    let trace_id = crate::trace::next_id();
+    crate::trace::spawn_begin(trace_id, true, env);
     let mut child = pair.slave.spawn_command(command_builder)?;
+    crate::trace::spawn_return(trace_id, child.process_id(), env);
     #[cfg(unix)]
     // portable-pty establishes the spawned PTY child as a new session leader on
     // Unix, so PID == PGID and we can reuse the pipe backend's process-group
@@ -193,6 +205,7 @@ async fn spawn_process_portable(
         stdout_tx,
         writer_rx,
         crate::unix_io::StdinCloseBehavior::SendEof,
+        trace_id,
     );
     #[cfg(not(unix))]
     let (reader_handle, writer_handle) = {
@@ -243,7 +256,17 @@ async fn spawn_process_portable(
     let wait_exit_code = Arc::clone(&exit_code);
     let wait_handle: JoinHandle<()> = tokio::task::spawn_blocking(move || {
         let code = match child.wait() {
-            Ok(status) => status.exit_code() as i32,
+            Ok(status) => {
+                crate::trace::emit(
+                    "child",
+                    "wait_return",
+                    &format!(
+                        "{{\"stream_id\":{trace_id},\"reported_code\":{},\"signal\":null,\"signal_evidence\":\"portable_pty_status\"}}",
+                        status.exit_code()
+                    ),
+                );
+                status.exit_code() as i32
+            }
             Err(_) => -1,
         };
         wait_exit_status.store(true, std::sync::atomic::Ordering::SeqCst);
@@ -318,6 +341,11 @@ async fn spawn_process_preserving_fds(
     let stdin = slave.try_clone()?;
     let stdout = slave.try_clone()?;
     let stderr = slave.try_clone()?;
+    let close_behavior = if inherited_fds.is_empty() {
+        crate::unix_io::StdinCloseBehavior::SendEof
+    } else {
+        crate::unix_io::StdinCloseBehavior::NoEof
+    };
     let inherited_fds = inherited_fds.to_vec();
 
     unsafe {
@@ -357,18 +385,17 @@ async fn spawn_process_preserving_fds(
             });
     }
 
+    let trace_id = crate::trace::next_id();
+    crate::trace::spawn_begin(trace_id, true, env);
     let mut child = command.spawn()?;
+    crate::trace::spawn_return(trace_id, Some(child.id()), env);
     drop(slave);
     let process_group_id = child.id();
 
     let (writer_tx, writer_rx) = mpsc::channel::<Vec<u8>>(128);
     let (stdout_tx, stdout_rx) = mpsc::channel::<Vec<u8>>(128);
     let (_stderr_tx, stderr_rx) = mpsc::channel::<Vec<u8>>(1);
-    let (reader_handle, writer_handle) = io.spawn(
-        stdout_tx,
-        writer_rx,
-        crate::unix_io::StdinCloseBehavior::NoEof,
-    );
+    let (reader_handle, writer_handle) = io.spawn(stdout_tx, writer_rx, close_behavior, trace_id);
 
     let (exit_tx, exit_rx) = oneshot::channel::<i32>();
     let exit_status = Arc::new(AtomicBool::new(false));
@@ -377,7 +404,19 @@ async fn spawn_process_preserving_fds(
     let wait_exit_code = Arc::clone(&exit_code);
     let wait_handle: JoinHandle<()> = tokio::task::spawn_blocking(move || {
         let code = match child.wait() {
-            Ok(status) => exit_code_from_status(status),
+            Ok(status) => {
+                use std::os::unix::process::ExitStatusExt;
+                crate::trace::emit(
+                    "child",
+                    "wait_return",
+                    &format!(
+                        "{{\"stream_id\":{trace_id},\"exit_code\":{},\"signal\":{}}}",
+                        status.code().map_or("null".into(), |v| v.to_string()),
+                        status.signal().map_or("null".into(), |v| v.to_string())
+                    ),
+                );
+                exit_code_from_status(status)
+            }
             Err(_) => -1,
         };
         wait_exit_status.store(true, std::sync::atomic::Ordering::SeqCst);
diff --git a/codex-rs/utils/pty/src/unix_io.rs b/codex-rs/utils/pty/src/unix_io.rs
index 315c2b6..ef3b6be 100644
--- a/codex-rs/utils/pty/src/unix_io.rs
+++ b/codex-rs/utils/pty/src/unix_io.rs
@@ -45,6 +45,7 @@ impl PtyIo {
         stdout_tx: mpsc::Sender<Vec<u8>>,
         mut writer_rx: mpsc::Receiver<Vec<u8>>,
         stdin_close: StdinCloseBehavior,
+        trace_id: u64,
     ) -> (JoinHandle<()>, JoinHandle<()>) {
         let fd = self.fd;
         let reader = Arc::clone(&fd);
@@ -55,14 +56,32 @@ impl PtyIo {
                     .async_io(Interest::READABLE, |mut file| file.read(&mut buf))
                     .await
                 {
-                    Ok(0) => break,
+                    Ok(0) => {
+                        crate::trace::stream(trace_id, "pty", &[]);
+                        break;
+                    }
                     Ok(count) => {
+                        crate::trace::stream(trace_id, "pty", &buf[..count]);
                         // Output caps may close the receiver before the child exits.
                         let _ = stdout_tx.send(buf[..count].to_vec()).await;
                     }
                     Err(err) if err.kind() == io::ErrorKind::Interrupted => continue,
                     // Unix PTYs may report EIO rather than EOF when the slave closes.
-                    Err(_) => break,
+                    Err(err) => {
+                        if err.raw_os_error() == Some(libc::EIO) {
+                            crate::trace::stream(trace_id, "pty", &[]);
+                        } else {
+                            crate::trace::emit(
+                                "io",
+                                "read_error",
+                                &format!(
+                                    "{{\"stream_id\":{trace_id},\"errno\":{}}}",
+                                    err.raw_os_error().map_or("null".into(), |v| v.to_string())
+                                ),
+                            );
+                        }
+                        break;
+                    }
                 }
             }
         });
