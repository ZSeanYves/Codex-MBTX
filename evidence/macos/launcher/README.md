# macOS launcher evidence

`moon run scripts/collect-macos.mbtx` creates a timestamped launcher comparison
run here by default. An optional output argument selects a new run directory.
Keep macOS evidence separate from Linux and do not treat it as online relay
evidence. No macOS measurement dataset is published in the current tree.
