# macOS launcher evidence

`moon run scripts/collect-macos.mbtx` writes immutable launcher comparison runs
here when given this directory as its output argument. Keep macOS evidence
separate from Linux and do not treat it as online relay evidence.
